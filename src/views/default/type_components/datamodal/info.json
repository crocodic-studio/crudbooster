{
  "title": "Data Modal",
  "type": "datamodal",
  "attribute": {
    "required": {
      "datamodal_table": "table name",
      "datamodal_columns": "column name, sparate with comma",
      "datamodal_size": {
        "type": "radio",
        "enum": [
          "large",
          "small"
        ]
      }
    },
    "optional": {
      "datamodal_where": "string where condition",
      "datamodal_module_path": "Module name",
      "datamodal_columns_alias_name": "Column name Alias , sparate with comma ex: 'Name Surname','Age' ",
      "style": "string inline stylesheet",
      "help": "string help-block"
    }
  }
}
